九、已知坑与预防
坑	原因	预防
DSH 只信任 loopback	安全设计，防止 RCE 暴露	用 --trusted-host 加白名单
端口占用 EADDRINUSE	手动 ssh 隧道占用端口	部署前 ss -tlnp 检查
插件激活失败导致 DSH 崩溃	依赖不存在的服务	出问题先看 journalctl，卸载问题插件
dict.get(key, default) 返回 None	key 存在但值为 null	用 (m.get(key) or "")