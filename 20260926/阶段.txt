已完成阶段（回顾）
阶段 0：硬件与基础设施 ✅
PVE 宿主机：i5-13500 + 52G DDR4 + 2T NVMe + 1.8T SATA SSD

AI 虚拟机：Ubuntu 24.04，8 核，24G 内存，64G 系统盘 + 1.5T NVMe + 1.8T SSD

RTX 5060 Ti 16G 直通给 AI 虚拟机

10G 网卡 SR-IOV 直通，10.0.0.2 专用通道

Tailscale 组网，aiserver.tail2fe40b.ts.net 远程访问

阶段 0.5：存储与文件服务 ✅
Samba 共享 /data 和 /mnt/ssd

FileBrowser 可视化面板（Docker，自启）

Windows 自动备份脚本（Pictures + Videos → 缓存区）

多用户权限（管理员 / 访客 / 家人）

阶段 0.8：模型部署 ✅
Ollama + 三个模型就位：

DSR1 14B（判断层）

Qwen 14B（润色层）

ASTRA 14B（执行层，原生工具调用）

显存管理（OLLAMA_MAX_LOADED_MODELS=1）

阶段 0.9：DSH 前端 ✅
DSH 0.1.5-rc.2 部署

手机端访问修复（--trusted-host）

中间层（FastAPI）跑通

日志系统（步骤流水账）

ASTRA 工具规划（不执行，显示待处理）

DSR1 → 分支 → Qwen / ASTRA 路由

从零到可用，你已经走完了整套基础设施搭建。

待办阶段（完整版）
阶段 1：中间层稳固化 🔵（当前）
1.1 从 DSH 的 156 条消息里过滤噪音，提取真实用户输入

1.2 用历史 dump 回放验证，覆盖边界场景

分支路由

DSR1 改为 WRITE / READ 二分


阶段 2：工具执行
2.1 ASTRA 输出改为短句格式（<call>name k=v</call>）

2.2 实装工具：bash / read_file / write_file / web_search / set_reminder / record_note

2.3 安全拦截：工具白名单、路径白名单、bash 命令解析

2.4 结果压缩（>50 行只留头 20 + 尾 10）

2.5 DSR1 结果检查接入（脚本客观 + DSR1 主观）

阶段 3：提示词工程
3.1 DSR1 意图分类提示词（加 Few-shot）

3.2 ASTRA 规划提示词（工具选择约束）

3.3 Qwen 润色提示词（防幻觉硬约束）

3.4 三个模型的 Few-shot 固化

阶段 4：记忆（基础）
4.1 短期记忆：当前会话 turns 存储

4.2 长期记忆：facts / preferences / todos 分层

4.3 基础向量库（选型 + 写入 + 检索）

4.4 记忆读写接口

阶段 5：信息采集（简版）
5.1 时间采集（系统时间）

5.2 地点采集（手机后台程序 → 服务器）

5.3 心情采集（DSR1 推断 + 必要时直接问）

5.4 组装"基础状态卡"

阶段 6：状态工程（简易）
6.1 用户状态：在哪、心情、作息

6.2 Agent 状态：清醒 / 假休眠 / 忙碌

6.3 状态调节输出：地点影响频率、深夜影响提示词、忙碌影响语气

6.4 短时情绪（起床气等，可被安慰平复）

阶段 7：主动消息
7.1 触发判断（基于状态卡）

7.2 调度（定时检查 + 避免打扰）

7.3 消息模板（不同场景不同语气）

7.4 与你角色的语气一致性

阶段 8：对外接口
8.1 择一先做（微信 / QQ / Discord）

8.2 输入适配层

8.3 输出适配层

8.4 与中间层对接

阶段 9：状态工程（全）
9.1 状态条：实时可视化 + 持久化

9.2 信息采集节奏动态调节（频率随状态变化）

9.3 用户社交活跃度、作息、健康多维状态

9.4 Agent 情绪维度（更长周期的"心情"）

阶段 10：记忆改良
10.1 容纳海量外部输入

10.2 跨平台记忆关联

10.3 记忆压缩与摘要

10.4 检索优化（相关性、时效性）

阶段 11：信息采集（全）
11.1 全平台消息采集

11.2 群聊消息总结与任务提取

11.3 个人信息卡完整化

11.4 异步处理管道

阶段 12：对外主动信息（主动社交）
12.1 独立账号

12.2 三种模式放开：代笔 / 独立人格 / 辅助建议

12.3 "她"在群里自主参与社交

12.4 当别人问她是什么时，由你决定怎么回应

[已完成] 硬件 → 存储 → 模型 → DSH → 中间层管道

1. 中间层稳固化          过滤 DSH 噪音，稳定提取用户输入
2. 工具执行              让"计划"变成"执行"（含安全拦截、结果压缩）
3. 提示词工程            DSR1 / ASTRA / Qwen 三个模型的提示词固化
4. 记忆（基础）          短期 turns + 长期 facts/preferences/todos
5. 信息采集（简版）      时间 / 地点 / 心情 → 组装基础状态卡
6. 状态工程（简易）      用户状态 + Agent 状态，调节输出倾向
7. 主动消息              基于状态卡判断"该不该联系、说什么"
8. 对外接口              微信 / QQ / Discord（择一先做）
9. 状态工程（全）        状态条、实时判断、信息采集节奏调节
10. 记忆改良             容纳海量外部输入
11. 信息采集（全）       全平台全量采集
12. 对外主动信息         代笔 / 独立人格 / 辅助建议（三条都放开）