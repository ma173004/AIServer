四、对话层：Qwen3（四次调用，一个模型）
调用	岗位	输入	输出	temp
Q1	情绪分析	用户原文 + 角色卡	用户情绪 + 角色情绪变化 + 影响评估	0.1
Q2a	状态与决策	Q1结果 + 用户卡 + 时间/地点 + 上下文	是否行动 + 是否调工具 + 调哪些 + 是否输出 + 是否升级到工程层	0.1
Q2b	统一草拟出口	Q2a结果 / 小工具结果 / 大工程验收结果 + 角色卡 + 用户卡	草稿文本	0.7
Q3	最终润色	草稿 + 角色卡	最终文本	0.7

Q3 硬约束：

不改数字 / 路径 / 事实

不加原文没有的信息

不假装执行（"她暂时无法接触现实"）


每层细节
Q1 · 情绪分析

分析用户当前情绪

评估对话对角色情绪的影响

写进用户卡

不做：决策、生成

Q2a · 状态与决策

结合时间/地点/状态判断是否行动（如"用户放学晚归 → 调定位"）

判断是否需要工具

判断是否需要输出给用户

可升级到工程层（升级条件：任务超出对话层工具能力）

不做：生成、情绪

Q2b · 统一草拟出口

接收三种来源的结果：

Q2a 判断"不调工具"的纯聊天
小工具执行结果
大工程验收后的工作流结果
结合角色卡 + 用户卡 + 上下文草拟一段回复

不做：判断、润色

Q3 · 最终润色

根据角色卡润色草稿

硬约束：

不改数字/路径/事实

不加原文没有的信息

不假装执行（"她暂时无法接触现实"）

不做：判断、加新事实